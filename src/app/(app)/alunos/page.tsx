import { AlunosView } from "@/domains/alunos/components/AlunosView";

/** Rota fina: delega toda a UI ao componente-raiz do domínio Alunos. */
export default function AlunosPage() {
  return <AlunosView />;
}
