import { AgendaView } from "@/domains/agenda/components/AgendaView";

/** Rota fina do domínio Agenda — apenas renderiza o componente-raiz. */
export default function AgendaPage() {
  return <AgendaView />;
}
