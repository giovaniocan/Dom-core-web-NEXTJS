import { FrequenciaView } from "@/domains/catraca/components/FrequenciaView";

/** Rota fina da frequência (domínio "catraca") — agrega a coleção acessos. */
export default function FrequenciaPage() {
  return <FrequenciaView />;
}
