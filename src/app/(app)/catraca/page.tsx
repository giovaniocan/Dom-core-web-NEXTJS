import { CatracaView } from "@/domains/catraca/components/CatracaView";

/** Rota fina do domínio "catraca" — painel ao vivo (coleção: acessos). */
export default function CatracaPage() {
  return <CatracaView />;
}
