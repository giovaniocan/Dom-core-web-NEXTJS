import { InadimplenciaView } from "@/domains/financeiro/components/InadimplenciaView";

/** Rota fina: renderiza a inadimplência (domínio Financeiro). */
export default function InadimplenciaPage() {
  return <InadimplenciaView />;
}
